import { InstrumentNamePipe } from './instrument-name.pipe';

describe('InstrumentNamePipe', () => {
  it('create an instance', () => {
    const pipe = new InstrumentNamePipe();
    expect(pipe).toBeTruthy();
  });
});
